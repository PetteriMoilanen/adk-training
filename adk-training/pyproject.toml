[project]
name = "adk-training"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "PetteriMoilanen", email = "115905059+PetteriMoilanen@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = [
    "google-adk>=2.1.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
adk-training = "adk_training:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
